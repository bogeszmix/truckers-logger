import { DateFormatTimePipe } from './date-format-time.pipe';

describe('DateFormatTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatTimePipe();
    expect(pipe).toBeTruthy();
  });
});
