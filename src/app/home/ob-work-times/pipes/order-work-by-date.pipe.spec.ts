import { OrderWorkByDatePipe } from './order-work-by-date.pipe';

describe('OrderWorkByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderWorkByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
